# Current Todo

Updated: 2026-04-19

## Parked Workstream
- Add unit tests for `AliasStateManager`
- Add unit tests for `FeedbackStateManager`
- Add unit/integration coverage for `SessionManager` prune and recovery behavior
- Add integration tests for `get_feedback`, `check_interrupts`, and plugin restart/reconnect flow
- Revisit whether the remaining prune/session-lifecycle cluster in `src/session-manager.ts` is still worth extracting after tests land

## Status
- Refactor stream is parked at a build-green checkpoint.
- No active code change is in progress.
- Current recommendation is tests before more refactors.
